# GitHub Actions Environment Configuration
# Copy this file and fill in your values

# VPS Configuration
# The IP address or hostname of your VPS
VPS_HOST=your.vps.ip.or.hostname

# SSH user on the VPS (usually ubuntu, debian, or root)
VPS_USER=ubuntu

# Full path to the application directory on VPS
VPS_APP_PATH=/opt/ha-dashboard

# Your domain name (used for health checks and notifications)
VPS_DOMAIN=your-domain.com

# Docker Registry (optional, only if you use Docker Hub)
DOCKER_USERNAME=your-docker-username
DOCKER_PASSWORD=your-docker-password-or-token

# Backend Environment Variables (used in CI/CD)
BACKEND_NODE_ENV=production
BACKEND_PORT=4000

# Frontend Environment Variables (used in CI/CD)
FRONTEND_VITE_API_URL=https://api.your-domain.com
FRONTEND_VITE_WS_URL=wss://api.your-domain.com

# Home Assistant Configuration
HA_URL=https://your-home-assistant-url.duckdns.org:8123
HA_TOKEN=your-long-lived-token-here

# Security
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production

# Database
POSTGRES_USER=ha_dashboard
POSTGRES_PASSWORD=your-secure-db-password
POSTGRES_DB=ha_dashboard

# Redis
REDIS_PASSWORD=your-secure-redis-password

# CORS
CORS_ORIGIN=https://your-domain.com
